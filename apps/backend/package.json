{
  "name": "@prodsprints/backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "start": "python -m uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "lint": "ruff check .",
    "format": "ruff format .",
    "test": "pytest",
    "build": "echo 'Backend build complete'"
  }
}
